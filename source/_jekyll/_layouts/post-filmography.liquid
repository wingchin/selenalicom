---
layout: base
---
<section id="filmography-post" class="default-padding">
  <div class="container">
    <div class="col-sm-10 col-sm-offset-1">
      <div class="well well-lg">
        <div class="row">
          <div class="col-sm-4">
            <figure>
              <img class="img-responsive" src="data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAIAAAADCAQAAAAT4xYKAAAADklEQVR42mNkAAJGOAEAAC0ABNxMS2YAAAAASUVORK5CYII=" data-src="{{ site.baseurl }}/assets/uploads/posters/filmography-poster-{{ page.slug }}.jpg" width="300" height="450" alt="{{ i.title }} poster">
            </figure>
          </div>

          <div class="col-sm-8">
            <div class="caption">
              <h1>{{ page.title }}</h1>
              <h4>{{ page.names.chinese }}</h4>

              <dl class="dl-horizontal">
                {% unless page.unreleased %}<dt>Broadcast date</dt>
                <dd>{{ page.broadcast.start | date: "%B %e, %Y" }} - {{ page.broadcast.end | date: "%B %e, %Y" }}</dd>{% endunless %}
                <dt>Genre(s)</dt>
                <dd>{{ page.genre | capitalize }}</dd>
                <dt>Running time</dt>
                <dd>{{ page.episodes }} episodes</dd>
                <dt>Starring</dt>
                <dd>{{ page.starring }}</dd>
                <dt>Produced by</dt>
                <dd>{{ page.producer }}</dd>
              </dl>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

{% if page.synopsis %}
<section class="white small-padding">
  <div class="container">
    <h2 class="text-center">Synopsis</h2>
    <p>{{ page.synopsis }}</p>
  </div>
</section>
{% endif %}

<section class="small-padding">
  <div class="container">
    <h2 class="text-center">Selena’s character(s)</h2>
    <div class="col-sm-8 col-sm-offset-2">
      {% for i in page.characters %}
        <div class="well well-sm">
          <div class="row">
            <div class="col-sm-3">
              <figure>
                <img class="img-responsive" src="data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAIAAAADCAQAAAAT4xYKAAAADklEQVR42mNkAAJGOAEAAC0ABNxMS2YAAAAASUVORK5CYII=" data-src="{{ site.baseurl }}/assets/uploads/characters/filmography-character-{{ page.slug }}-{{ forloop.index }}.jpg" width="300" height="450" alt="Selena as {{ i.fullname }}">
              </figure>
            </div>
            <div class="col-sm-9">
              {% if page.role == 'guest' %} <span class="label label-primary pull-right">guest role</span>{% endif %}
              <div class="caption">
                <h4>{{ i.fullname }}</h4>
                <dl class="dl-horizontal">
                  {% if i.altname %}
                    <dt>Also known as</dt>
                    <dd>{{ i.altname }}</dd>
                  {% endif %}

                  {% if i.age %}
                    <dt>Age</dt>
                    <dd>{{ i.age }} years</dd>
                  {% endif %}

                  {% if i.identity %}
                    <dt>Identity</dt>
                    <dd>{{ i.identity }}</dd>
                  {% endif %}

                  <dt>Appearance</dt>
                  <dd>Episode {{ i.appearance }}</dd>

    <!--
                  {% if i.personality %}
                    <dt>Personality</dt>
                    <dd>{{ i.personality }}</dd>
                  {% endif %}

                  {% if i.background %}
                    <dt>Background</dt>
                    <dd>{{ i.background }}</dd>
                  {% endif %}

                  {% if i.happenings %}
                    <dt>Happenings</dt>
                    <dd>{{ i.happenings }}</dd>
                  {% endif %}
    -->
                </dl>
              </div>
            </div>
          </div>
        </div>
      {% endfor %}
    </div>
  </div>
</section>